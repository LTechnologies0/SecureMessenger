<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose-only UI: ComponentActivity accepts platform Material themes.
         Keep AppCompat parent so any future AppCompat widgets stay safe. -->
    <style name="Theme.SecureMessenger" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
